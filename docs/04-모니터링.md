# 4. 모니터링

## 상태: TODO — 미구현

DataMonitor PoC(`DataMonitor-hyunjunchoi-21008753`)에서 검증한 콘솔 모니터링 방식을 이 프로젝트에 통합 예정. 아래는 빈 스케마이며, 실제 요구사항은 이 메뉴를 개발하는 시점에 사용자와 논의 후 채운다.

## 개요 (미정)

- 목적: 주문량/재고량 등 현황을 콘솔에서 확인하는 기능으로 추정 (README 기준: "모니터링 (주문량 / 재고량)")

## 논의 필요 항목

- [ ] 무엇을 모니터링할지 (주문 상태별 건수? 시료별 재고? 둘 다?)
- [ ] 실시간 갱신(DataMonitor PoC의 live refresh)이 필요한지, 1회성 스냅샷 조회로 충분한지
- [ ] 데이터 소스: 기존 `SampleRepository`/`OrderRepository`를 그대로 쓸지, 별도 집계 쿼리가 필요한지
- [ ] 화면 구성 (표 형식? 요약 수치?)

## 데이터/상태 (미정)

TBD — 논의 후 작성.

## TDD 체크리스트 (테스트 대상)

TBD — `Plan.md` 작성 시 채움.

## 완료 기준

TBD.

## 관련 파일 (예상)

`app/monitor.py`(신규 예상), `app/cli.py`(메뉴 추가). 참고: `DataMonitor-hyunjunchoi-21008753/app/monitor.py`, `app/db.py`
