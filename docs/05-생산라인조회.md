# 5. 생산 라인 조회

## 상태: TODO — 미구현

현재 `PRD.md`의 도메인 모델에는 "생산 라인"이라는 개념 자체가 없다. 이 메뉴를 개발하기 전에 도메인 모델 확장이 필요할 가능성이 높다.

## 개요 (미정)

- 목적: `IN_PRODUCTION` 상태인 주문들이 어느 생산 라인에서 처리되고 있는지 조회하는 기능으로 추정.

## 논의 필요 항목

- [ ] "생산 라인"을 새 엔티티로 모델링할지(예: `ProductionLine(line_id, capacity, ...)`), 아니면 `Order`에 `line_id` 필드만 추가할지
- [ ] 생산 시작(`start_production`, 현재 3번 메뉴가 아니라 `OrderService.start_production`에만 있고 별도 CLI 메뉴 없음) 처리를 이 메뉴에서 담당할지, 별도로 둘지
- [ ] 라인 배정 로직이 필요한지(자동 배정 vs 수동 지정), 아니면 조회 전용 기능인지
- [ ] 현재 `main 메뉴`에는 "생산 시작"이 없고 `cli.py`의 메뉴 6번이 `start_production`인데, 사용자가 말한 6개 메뉴 구조(시료관리/시료주문/주문승인거절/모니터링/생산라인조회/출고처리)와 현재 `cli.py` 메뉴가 다름 — 이 문서 작업 시 메뉴 재구성 여부도 함께 정리 필요

## 데이터/상태 (미정)

TBD — 논의 후 작성.

## TDD 체크리스트 (테스트 대상)

TBD — `Plan.md` 작성 시 채움.

## 완료 기준

TBD.

## 관련 파일 (예상)

`app/models.py`(확장 가능성), `app/repository.py`(확장 가능성), `app/cli.py`(메뉴 추가/재구성)
